{
  "mcpServers": {
    "sprint-orchestrator": {
      "command": "sh",
      "args": [
        "-c",
        "cd \"${CLAUDE_PLUGIN_ROOT}/mcp\" && [ -d node_modules ] || npm install --no-audit --no-fund --silent; exec npx tsx src/index.ts"
      ],
      "env": {}
    }
  }
}
